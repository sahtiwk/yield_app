SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_PUBLISHABLE_KEY=replace-with-local-publishable-or-anon-key

# Never put SUPABASE_SECRET_KEY, SUPABASE_SERVICE_ROLE_KEY, or JWT_SECRET in a
# Flutter application. Those belong only on trusted server-side code.
